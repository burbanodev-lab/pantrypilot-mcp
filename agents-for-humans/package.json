{
  "name": "pantrypilot-strands-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PantryPilot Strands agent for the AWS Agents for Humans Hackathon — Strands orchestrates the PantryPilot MCP tool surface end to end.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "agent": "node dist/agent.js",
    "judge-check": "node dist/judge-check.js",
    "verify": "npm run build && npm run judge-check"
  },
  "dependencies": {
    "@strands-agents/sdk": "1.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.9.0"
  }
}
